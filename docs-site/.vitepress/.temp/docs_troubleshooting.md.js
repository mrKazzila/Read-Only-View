import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Why Is My Obsidian Note Read-Only or Still Editable?","description":"Troubleshoot Read Only View rules when a note stays editable, remains read-only after disabling an Include, or does not reflect saved settings.","frontmatter":{"title":"Why Is My Obsidian Note Read-Only or Still Editable?","description":"Troubleshoot Read Only View rules when a note stays editable, remains read-only after disabling an Include, or does not reflect saved settings."},"headers":[],"relativePath":"docs/troubleshooting.md","filePath":"docs/troubleshooting.md"}');
const _sfc_main = { name: "docs/troubleshooting.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="why-is-my-obsidian-note-read-only-or-still-editable" tabindex="-1">Why Is My Obsidian Note Read-Only or Still Editable? <a class="header-anchor" href="#why-is-my-obsidian-note-read-only-or-still-editable" aria-label="Permalink to &quot;Why Is My Obsidian Note Read-Only or Still Editable?&quot;">​</a></h1><p>Start with <strong>Path tester</strong> in <strong>Settings → Read Only View</strong>. Enter the specific Markdown note path and check its resolved path, matching rules, and final <strong>Read-only</strong> or <strong>Editable</strong> status. This separates a rule-configuration problem from a view that has not reflected the configuration yet.</p><h2 id="still-editable" tabindex="-1">Why is my note still editable after I added an Include? <a class="header-anchor" href="#still-editable" aria-label="Permalink to &quot;Why is my note still editable after I added an Include? {#still-editable}&quot;">​</a></h2><p>An include protects a Markdown note only while the plugin is enabled and no enabled exclude matches. Check these points in order:</p><ol><li>Confirm the global <strong>Enabled</strong> toggle is on and the file is a Markdown note.</li><li>Look for an <strong>Exclude</strong> match in Path tester. Excludes always win; adding another include cannot override one.</li><li>In <strong>Only matched paths</strong>, confirm the Include row is enabled and its source resolves without an inline error.</li><li>Check <strong>Advanced → Matching</strong>. Use <code>Reference/</code> for a folder with glob matching off, or <code>Reference/**</code> with glob matching on. With globs off, <code>*</code> and <code>?</code> are literal characters.</li><li>Check spelling and capitalization. <strong>Case sensitive</strong> is on by default. Imported URLs and system paths must still resolve to existing items in the current vault.</li><li>If the note was moved or renamed, update the saved path. Imported rules do not automatically follow renames.</li></ol><p>For example, Include <code>Reference/</code> and Exclude <code>Reference/Drafts/</code> leave <code>Reference/Drafts/Plan.md</code> editable. Disable or narrow that exclude if you want the note protected.</p><p>If settings show a rule-limit warning, review which rules were ignored. See <a href="./path-rules#verify-the-result">Path rules</a> for the limits.</p><h2 id="unexpectedly-read-only" tabindex="-1">Why is my note read-only when I want to edit it? <a class="header-anchor" href="#unexpectedly-read-only" aria-label="Permalink to &quot;Why is my note read-only when I want to edit it? {#unexpectedly-read-only}&quot;">​</a></h2><p>Check <strong>Mode</strong> first. New installations use <strong>All Markdown files</strong>, which protects Markdown notes even without include rules. Choose <strong>Only matched paths</strong> for selected-path protection, or add an enabled exclude for an exception.</p><p>In <strong>Only matched paths</strong>, check every matching include in Path tester. A broad include such as <code>Reference/</code> in prefix mode, or <code>**</code> in glob mode, may protect the note alongside its individual rule.</p><p>For example, a vault in <strong>All Markdown files</strong> mode needs an Exclude for <code>Daily Notes/</code> to leave that folder editable with glob matching off. With glob matching on, use <code>Daily Notes/**</code>. See the <a href="./../guides/make-all-notes-read-only#except-daily-notes">daily notes example</a>.</p><h2 id="disabled-include" tabindex="-1">Why does disabling an Include not let me edit the note? <a class="header-anchor" href="#disabled-include" aria-label="Permalink to &quot;Why does disabling an Include not let me edit the note? {#disabled-include}&quot;">​</a></h2><p>Either <strong>All Markdown files</strong> is active or another enabled include still matches. Disabling one rule does not override those other sources of protection.</p><p>For a temporary exception, keep an <strong>Exclude</strong> row for the note: enable the exclude while editing, then disable it to restore protection. Its path stays saved. To pause enforcement for every note, run <strong>Disable read-only mode</strong> and later <strong>Enable read-only mode</strong>. See <a href="./path-rules#temporarily-disable-rules">temporarily disabling rules</a>.</p><h2 id="view-not-updated" tabindex="-1">Why does Path tester disagree with the open note? <a class="header-anchor" href="#view-not-updated" aria-label="Permalink to &quot;Why does Path tester disagree with the open note? {#view-not-updated}&quot;">​</a></h2><p>Confirm you tested the actual note path and waited for <strong>Saved.</strong> after editing rules. If settings report <strong>Save failed.</strong>, the changes have not saved successfully.</p><p>Run <strong>Re-apply rules now</strong> from the Command Palette, then reopen the note if necessary. An <strong>Editable</strong> result means Read Only View is not enforcing protection; you may still need to switch the note from Reading view to editing yourself.</p><p>If Path tester reports <strong>Read-only</strong> but editing remains possible in a hover, embedded, or nonstandard view, that context may depend on Obsidian internals. Protection is not guaranteed in every such view, and it does not prevent another plugin or application from modifying files.</p><p>If the problem continues, <a href="https://github.com/mrKazzila/Read-Only-View/issues" target="_blank" rel="noreferrer">report an issue</a> with your Obsidian and plugin versions, platform, mode, matching settings, and a minimal rule example. Use anonymized paths and describe whether it occurs in a normal note tab or a preview. See <a href="./path-tester">Path tester</a> for the available diagnostics.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("docs/troubleshooting.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const troubleshooting = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  troubleshooting as default
};
