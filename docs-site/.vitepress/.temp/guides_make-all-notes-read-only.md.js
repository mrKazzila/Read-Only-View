import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"How to Keep All Obsidian Notes in Reading View","description":"Make all Markdown notes in an Obsidian vault read-only using All Markdown files mode or an Include rule with **, while keeping editable exceptions.","frontmatter":{"title":"How to Keep All Obsidian Notes in Reading View","description":"Make all Markdown notes in an Obsidian vault read-only using All Markdown files mode or an Include rule with **, while keeping editable exceptions."},"headers":[],"relativePath":"guides/make-all-notes-read-only.md","filePath":"guides/make-all-notes-read-only.md"}');
const _sfc_main = { name: "guides/make-all-notes-read-only.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="how-to-keep-all-obsidian-notes-in-reading-view" tabindex="-1">How to Keep All Obsidian Notes in Reading View <a class="header-anchor" href="#how-to-keep-all-obsidian-notes-in-reading-view" aria-label="Permalink to &quot;How to Keep All Obsidian Notes in Reading View&quot;">​</a></h1><p>To keep every Markdown note in your Obsidian vault in Reading view, enable Read Only View and select <strong>All Markdown files</strong>. You can also use an <strong>Include</strong> rule with <code>**</code> when glob matching is enabled.</p><p>Both options help prevent accidental edits in Obsidian. They do not change filesystem permissions or protect attachments and changes made by other applications.</p><h2 id="how-do-i-make-my-entire-obsidian-vault-read-only" tabindex="-1">How do I make my entire Obsidian vault read-only? <a class="header-anchor" href="#how-do-i-make-my-entire-obsidian-vault-read-only" aria-label="Permalink to &quot;How do I make my entire Obsidian vault read-only?&quot;">​</a></h2><ol><li>Open <strong>Settings → Community plugins → Browse</strong>, search for <strong>Read Only View</strong>, then <strong>Install</strong> and <strong>Enable</strong> it.</li><li>Open <strong>Settings → Read Only View</strong> and keep <strong>Enabled</strong> on.</li><li>Select <strong>All Markdown files</strong>. This is the default for new installations.</li><li>Review existing <strong>Exclude</strong> rules: any enabled matching exclude leaves the note editable.</li><li>Open a Markdown note, or run <strong>Re-apply rules now</strong> to apply the configuration across open Markdown notes.</li></ol><p>No include rules are required. Saved include rules remain available but inactive in this mode. <strong>Use glob patterns</strong> does not need to be enabled for the all-Markdown preset; it still controls how ordinary exclude rules match.</p><h2 id="can-i-match-all-notes-with-an-include-rule-and" tabindex="-1">Can I match all notes with an Include rule and **? <a class="header-anchor" href="#can-i-match-all-notes-with-an-include-rule-and" aria-label="Permalink to &quot;Can I match all notes with an Include rule and **?&quot;">​</a></h2><p>Yes. The <code>**</code> glob matches paths at every depth, including notes directly in the vault root.</p><ol><li>Select <strong>Only matched paths</strong> and keep the global <strong>Enabled</strong> toggle on.</li><li>Under <strong>Advanced → Matching</strong>, turn on <strong>Use glob patterns</strong>.</li><li>Under <strong>Path rules</strong>, add an enabled <strong>Include</strong> with <strong>Value</strong> set to <code>**</code>.</li><li>Wait for <strong>Saved.</strong> and check a note with <a href="./../docs/path-tester">Path tester</a>.</li></ol><table tabindex="0"><thead><tr><th>Tested path</th><th>Result with Include <code>**</code> and no matching exclude</th></tr></thead><tbody><tr><td><code>Home.md</code></td><td>Read-only</td></tr><tr><td><code>Reference/Handbook.md</code></td><td>Read-only</td></tr><tr><td><code>Archive/2025/Meeting.md</code></td><td>Read-only</td></tr></tbody></table><p>Only Markdown notes are protected even though <code>**</code> can match other path strings. With <strong>Use glob patterns</strong> off, <code>**</code> is literal text and does not select all notes. A single <code>*</code> cannot cross folder separators.</p><div class="tip custom-block"><p class="custom-block-title">Review existing rules when enabling globs</p><p>Glob matching is a shared setting for ordinary path rules. Change folder prefixes such as <code>Reference/</code> to <code>Reference/**</code> when switching to glob mode. Exact imported note rules keep their exact-file behavior.</p></div><h2 id="how-do-i-keep-some-notes-editable-in-a-read-only-vault" tabindex="-1">How do I keep some notes editable in a read-only vault? <a class="header-anchor" href="#how-do-i-keep-some-notes-editable-in-a-read-only-vault" aria-label="Permalink to &quot;How do I keep some notes editable in a read-only vault?&quot;">​</a></h2><p>Add enabled <strong>Exclude</strong> rules. Excludes take precedence in both approaches.</p><table tabindex="0"><thead><tr><th>Matching mode</th><th>Exclude value</th><th>Editable exception</th></tr></thead><tbody><tr><td>Glob matching off</td><td><code>Inbox/</code></td><td>Markdown notes in Inbox and its subfolders</td></tr><tr><td>Glob matching on</td><td><code>Inbox/**</code></td><td>Markdown notes in Inbox and its subfolders</td></tr><tr><td>Either mode</td><td><code>Reference/Working notes.md</code></td><td>The named note; ordinary paths remain prefixes when globs are off</td></tr></tbody></table><p>For an exact-file exception regardless of matching mode, paste the existing note&#39;s Obsidian URL into an <strong>Exclude</strong> row. See <a href="./../docs/path-rules#import-a-note-or-folder">supported path sources</a>.</p><h2 id="except-daily-notes" tabindex="-1">How do I make my vault read-only except for daily notes? <a class="header-anchor" href="#except-daily-notes" aria-label="Permalink to &quot;How do I make my vault read-only except for daily notes? {#except-daily-notes}&quot;">​</a></h2><p>Use <strong>All Markdown files</strong> with an enabled <strong>Exclude</strong> for the folder where you keep daily notes. For example, protect reference material while keeping your journal and quick captures editable:</p><ol><li>Open <strong>Settings → Read Only View</strong>, keep <strong>Enabled</strong> on, and select <strong>All Markdown files</strong>.</li><li>With <strong>Use glob patterns</strong> off, add these enabled <strong>Exclude</strong> rows under <strong>Path rules</strong>:</li></ol><table tabindex="0"><thead><tr><th>Type</th><th>Value</th></tr></thead><tbody><tr><td>Exclude</td><td><code>Daily Notes/</code></td></tr><tr><td>Exclude</td><td><code>Inbox/</code></td></tr></tbody></table><ol start="3"><li>Replace the example folder names with the actual vault-relative paths you use. Read Only View matches paths; it does not detect daily notes by date or automatically read another plugin&#39;s folder setting.</li><li>Wait for <strong>Saved.</strong> and check representative notes with <a href="./../docs/path-tester">Path tester</a>.</li></ol><table tabindex="0"><thead><tr><th>Tested path</th><th>Expected result with only the rules above</th></tr></thead><tbody><tr><td><code>Daily Notes/2026-09-18.md</code></td><td>Editable</td></tr><tr><td><code>Daily Notes/2026/09/18.md</code></td><td>Editable</td></tr><tr><td><code>Inbox/Quick capture.md</code></td><td>Editable</td></tr><tr><td><code>Reference/Handbook.md</code></td><td>Read-only</td></tr><tr><td><code>Home.md</code></td><td>Read-only</td></tr></tbody></table><p>New Markdown notes created under the excluded folders also match those folder rules. A daily note saved elsewhere does not get an exception just because it is a daily note.</p><p>If <strong>Use glob patterns</strong> is already on, use <code>Daily Notes/**</code> and <code>Inbox/**</code> instead. These excludes also work with <strong>Only matched paths</strong> and Include <code>**</code>. Review existing rules before changing the shared matching mode.</p><h2 id="can-i-temporarily-allow-editing-without-deleting-my-rules" tabindex="-1">Can I temporarily allow editing without deleting my rules? <a class="header-anchor" href="#can-i-temporarily-allow-editing-without-deleting-my-rules" aria-label="Permalink to &quot;Can I temporarily allow editing without deleting my rules?&quot;">​</a></h2><p>Run <strong>Disable read-only mode</strong>, switch into editing, then run <strong>Enable read-only mode</strong> when finished. This pauses protection globally and does not resume automatically.</p><p>If you use the <code>**</code> include in <strong>Only matched paths</strong>, you can instead uncheck that row&#39;s <strong>Enabled</strong> checkbox and recheck it later. Other enabled includes may still protect some notes. Disabling an include does not pause <strong>All Markdown files</strong> mode. See <a href="./../docs/path-rules#temporarily-disable-rules">temporarily disabling path rules</a>.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guides/make-all-notes-read-only.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const makeAllNotesReadOnly = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  makeAllNotesReadOnly as default
};
