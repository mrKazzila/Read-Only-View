import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"How to Make a Folder Read-Only in Obsidian","description":"Keep Markdown notes in a reference or archive folder in Reading view, with include rules for folders and exclude rules for one editable note or a drafts subfolder.","frontmatter":{"title":"How to Make a Folder Read-Only in Obsidian","description":"Keep Markdown notes in a reference or archive folder in Reading view, with include rules for folders and exclude rules for one editable note or a drafts subfolder."},"headers":[],"relativePath":"guides/make-folder-read-only.md","filePath":"guides/make-folder-read-only.md"}');
const _sfc_main = { name: "guides/make-folder-read-only.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="how-to-make-a-folder-read-only-in-obsidian" tabindex="-1">How to Make a Folder Read-Only in Obsidian <a class="header-anchor" href="#how-to-make-a-folder-read-only-in-obsidian" aria-label="Permalink to &quot;How to Make a Folder Read-Only in Obsidian&quot;">​</a></h1><p>Add a folder path such as <code>Reference/</code> as an <strong>Include</strong> rule in Read Only View. With ordinary path matching, Markdown notes inside that folder and its subfolders stay in Reading view.</p><h2 id="protect-a-reference-or-archive-folder" tabindex="-1">Protect a reference or archive folder <a class="header-anchor" href="#protect-a-reference-or-archive-folder" aria-label="Permalink to &quot;Protect a reference or archive folder&quot;">​</a></h2><ol><li><a href="./make-note-read-only#protect-a-reference-note">Install and enable Read Only View</a>.</li><li>Open <strong>Settings → Read Only View</strong>. Keep <strong>Enabled</strong> on and select <strong>Only matched paths</strong>.</li><li>Under <strong>Advanced → Matching</strong>, keep <strong>Use glob patterns</strong> off.</li><li>Under <strong>Path rules</strong>, add an enabled <strong>Include</strong> row with <strong>Value</strong> set to <code>Reference/</code>.</li><li>Wait for <strong>Saved.</strong> and open a Markdown note inside that folder.</li></ol><p>Use a vault-relative path, not the vault&#39;s name. Keep the trailing <code>/</code> to make the folder intent clear. For an archive, use <code>Archive/</code>; for a nested folder, use <code>Notes/Summaries/</code>.</p><h2 id="leave-one-note-editable" tabindex="-1">How do I make a folder read-only but leave one note editable? <a class="header-anchor" href="#leave-one-note-editable" aria-label="Permalink to &quot;How do I make a folder read-only but leave one note editable? {#leave-one-note-editable}&quot;">​</a></h2><p>Add an <strong>Exclude</strong> for that note alongside the folder&#39;s <strong>Include</strong>. With <strong>Only matched paths</strong> selected and <strong>Use glob patterns</strong> off, enable both rows:</p><table tabindex="0"><thead><tr><th>Type</th><th>Value</th></tr></thead><tbody><tr><td>Include</td><td><code>Reference/</code></td></tr><tr><td>Exclude</td><td><code>Reference/Working notes.md</code></td></tr></tbody></table><p><code>Reference/Handbook.md</code> and <code>Reference/Policies/Security.md</code> stay in Reading view, while <code>Reference/Working notes.md</code> remains editable. Excludes always win, even when an include also matches.</p><p>Ordinary file paths use prefix matching with glob matching off. To exempt exactly one existing note, paste its Obsidian URL into the exclude row instead. With glob matching on, use <code>Reference/**</code> for the folder include and <code>Reference/Working notes.md</code> for the note exclude.</p><p>Check both the exception and another note using <a href="./../docs/path-tester">Path tester</a>. To protect the exception again without retyping its path, uncheck the exclude row&#39;s <strong>Enabled</strong> checkbox; recheck it when you want to edit that note again.</p><h2 id="leave-drafts-editable" tabindex="-1">Leave drafts editable <a class="header-anchor" href="#leave-drafts-editable" aria-label="Permalink to &quot;Leave drafts editable&quot;">​</a></h2><p>Add these two rules:</p><table tabindex="0"><thead><tr><th>Type</th><th>Value</th></tr></thead><tbody><tr><td>Include</td><td><code>Reference/</code></td></tr><tr><td>Exclude</td><td><code>Reference/Drafts/</code></td></tr></tbody></table><p>The resulting behavior is:</p><table tabindex="0"><thead><tr><th>Note</th><th>Result</th></tr></thead><tbody><tr><td><code>Reference/Handbook.md</code></td><td>Read-only</td></tr><tr><td><code>Reference/Policies/Security.md</code></td><td>Read-only</td></tr><tr><td><code>Reference/Drafts/New policy.md</code></td><td>Editable</td></tr><tr><td><code>Inbox/Capture.md</code></td><td>Editable, unless another include rule matches</td></tr></tbody></table><p>An enabled <strong>Exclude</strong> always wins, even if another include rule also matches. Check any note with <a href="./../docs/path-tester">Path tester</a>.</p><h2 id="protect-everything-except-a-working-folder" tabindex="-1">Protect everything except a working folder <a class="header-anchor" href="#protect-everything-except-a-working-folder" aria-label="Permalink to &quot;Protect everything except a working folder&quot;">​</a></h2><p>If almost the whole vault is for reading, select <strong>All Markdown files</strong> and add an <strong>Exclude</strong> rule for <code>Inbox/</code>. Include rules are saved but inactive in this mode.</p><p>If you enable glob matching, use <code>Reference/**</code> instead of the plain folder rule. See <a href="./../docs/path-rules#glob-patterns">Path rules</a> before changing the matching mode, because it applies to ordinary path rules throughout the configuration.</p><h2 id="without-subfolders" tabindex="-1">How do I make only the notes directly in a folder read-only, without subfolders? <a class="header-anchor" href="#without-subfolders" aria-label="Permalink to &quot;How do I make only the notes directly in a folder read-only, without subfolders? {#without-subfolders}&quot;">​</a></h2><p>Use the glob <code>Reference/*.md</code> in <strong>Only matched paths</strong> mode. A single <code>*</code> cannot cross <code>/</code>, so it selects Markdown notes directly inside <code>Reference/</code> without selecting notes in nested folders.</p><ol><li>Keep the global <strong>Enabled</strong> toggle on and select <strong>Only matched paths</strong>.</li><li>Under <strong>Advanced → Matching</strong>, turn on <strong>Use glob patterns</strong>.</li><li>Add an enabled <strong>Include</strong> with <strong>Value</strong> set to <code>Reference/*.md</code>.</li><li>Disable broader includes such as <code>Reference/**</code> or <code>**</code> if you want the subfolders to remain editable.</li><li>Wait for <strong>Saved.</strong> and test both a direct note and a nested note with <a href="./../docs/path-tester">Path tester</a>.</li></ol><table tabindex="0"><thead><tr><th>Note</th><th>With Include <code>Reference/*.md</code></th><th>With Include <code>Reference/**</code></th></tr></thead><tbody><tr><td><code>Reference/Handbook.md</code></td><td>Read-only</td><td>Read-only</td></tr><tr><td><code>Reference/Policies/Security.md</code></td><td>Editable</td><td>Read-only</td></tr><tr><td><code>Inbox/Capture.md</code></td><td>Editable</td><td>Editable</td></tr></tbody></table><p>This comparison assumes <strong>Only matched paths</strong>, glob matching on, and only the include shown in each column, with no excludes. In <strong>All Markdown files</strong>, nested Markdown notes remain protected unless excluded.</p><p>Glob matching applies to all ordinary path rules. Review existing folder rules when turning it on: a prefix such as <code>Inbox/</code> needs to become <code>Inbox/**</code> to continue matching its contents. With glob matching off, <code>Reference/*.md</code> treats <code>*</code> literally and will not behave as this example describes.</p><h2 id="what-a-folder-rule-does-not-lock" tabindex="-1">What a folder rule does not lock <a class="header-anchor" href="#what-a-folder-rule-does-not-lock" aria-label="Permalink to &quot;What a folder rule does not lock&quot;">​</a></h2><p>The rule affects Markdown note views, not the directory&#39;s filesystem permissions. It does not protect PDFs, images, other attachments, or changes made by external tools. Review rules after moving or renaming notes and folders; imported paths do not automatically track renames.</p><p>For reading on a phone or tablet, see <a href="./mobile-reading-view">Reading view on mobile</a>.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guides/make-folder-read-only.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const makeFolderReadOnly = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  makeFolderReadOnly as default
};
