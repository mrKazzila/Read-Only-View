import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Keep Obsidian Notes in Reading View on Mobile","description":"Use Read Only View on phones and tablets to keep reference notes in Reading view, configure folder rules, and allow deliberate edits.","frontmatter":{"title":"Keep Obsidian Notes in Reading View on Mobile","description":"Use Read Only View on phones and tablets to keep reference notes in Reading view, configure folder rules, and allow deliberate edits."},"headers":[],"relativePath":"guides/mobile-reading-view.md","filePath":"guides/mobile-reading-view.md"}');
const _sfc_main = { name: "guides/mobile-reading-view.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="keep-obsidian-notes-in-reading-view-on-mobile" tabindex="-1">Keep Obsidian Notes in Reading View on Mobile <a class="header-anchor" href="#keep-obsidian-notes-in-reading-view-on-mobile" aria-label="Permalink to &quot;Keep Obsidian Notes in Reading View on Mobile&quot;">​</a></h1><p>Read Only View supports Obsidian on phones and tablets as well as desktop. Use it for notes you mainly consume: reference material, checklists, summaries, or an archive.</p><p>An accidental switch into editing can open the keyboard and interrupt reading. The plugin returns matching Markdown notes to Reading view and blocks editor input in supported editors. It does not promise to suppress every keyboard appearance or control every plugin-provided view.</p><h2 id="set-up-a-reading-folder" tabindex="-1">Set up a reading folder <a class="header-anchor" href="#set-up-a-reading-folder" aria-label="Permalink to &quot;Set up a reading folder&quot;">​</a></h2><ol><li>In Obsidian, open <strong>Settings → Community plugins → Browse</strong>. Search for <strong>Read Only View</strong>, then <strong>Install</strong> and <strong>Enable</strong> it.</li><li>Open <strong>Settings → Read Only View</strong>. Keep <strong>Enabled</strong> on and select <strong>Only matched paths</strong>.</li><li>Under <strong>Path rules</strong>, add an enabled <strong>Include</strong> rule for <code>Reference/</code>.</li><li>Keep <strong>Use glob patterns</strong> off under <strong>Advanced → Matching</strong>.</li><li>Wait for <strong>Saved.</strong>, then open a note in that folder.</li></ol><p>Use <a href="./../docs/path-tester">Path tester</a> with a specific note such as <code>Reference/Travel checklist.md</code> to check the result. On narrow screens, settings controls and rule rows use a stacked layout.</p><p>For an entire vault used mainly for reading, keep <strong>All Markdown files</strong> selected and add <strong>Exclude</strong> rules for notes you need to edit.</p><h2 id="use-portable-paths" tabindex="-1">Use portable paths <a class="header-anchor" href="#use-portable-paths" aria-label="Permalink to &quot;Use portable paths&quot;">​</a></h2><p>Vault-relative paths work on desktop and mobile. Obsidian URLs must resolve to an existing Markdown note in the current vault.</p><p>Importing a new absolute system path is desktop-only. Once imported, the rule stores its resolved vault-relative path, so that saved rule remains portable to mobile. You can enter the vault-relative path directly on your phone; no system path is needed.</p><h2 id="edit-when-you-intend-to" tabindex="-1">Edit when you intend to <a class="header-anchor" href="#edit-when-you-intend-to" aria-label="Permalink to &quot;Edit when you intend to&quot;">​</a></h2><p>Run <strong>Disable read-only mode</strong>, switch to editing, and make your change. Then run <strong>Enable read-only mode</strong>. The disable command pauses enforcement for all notes, not just the current one.</p><p>For a permanent writing area, add an <strong>Exclude</strong> rule such as <code>Reference/Drafts/</code>. See <a href="./make-folder-read-only">folder rules</a>.</p><h2 id="limits-and-privacy" tabindex="-1">Limits and privacy <a class="header-anchor" href="#limits-and-privacy" aria-label="Permalink to &quot;Limits and privacy&quot;">​</a></h2><p>Only Markdown notes are affected. The plugin does not change file permissions or prevent external applications from editing notes. Rule matching stays local, and the plugin makes no network requests. Installation and updates are separate from normal plugin operation.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guides/mobile-reading-view.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const mobileReadingView = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  mobileReadingView as default
};
