import { ssrRenderAttrs, ssrRenderAttr } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const _imports_0 = "/Read-Only-View/assets/Read-Only-View-path-rules-1200x800.BCpXUl9n.png";
const __pageData = JSON.parse('{"title":"Path Rules for Read Only View","description":"Learn how Include and Exclude rules, folder prefixes, exact imported notes, glob patterns, and matching precedence work in Read Only View.","frontmatter":{"title":"Path Rules for Read Only View","description":"Learn how Include and Exclude rules, folder prefixes, exact imported notes, glob patterns, and matching precedence work in Read Only View."},"headers":[],"relativePath":"docs/path-rules.md","filePath":"docs/path-rules.md"}');
const _sfc_main = { name: "docs/path-rules.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="path-rules" tabindex="-1">Path rules <a class="header-anchor" href="#path-rules" aria-label="Permalink to &quot;Path rules&quot;">​</a></h1><p><strong>Path rules</strong> select which Markdown notes stay in Reading view. Open <strong>Settings → Read Only View</strong> to add, disable, or remove a rule.</p><h2 id="modes-and-precedence" tabindex="-1">Modes and precedence <a class="header-anchor" href="#modes-and-precedence" aria-label="Permalink to &quot;Modes and precedence&quot;">​</a></h2><p>With <strong>Enabled</strong> on, the plugin evaluates a Markdown note in this order:</p><ol><li>An enabled <strong>Exclude</strong> match leaves it editable.</li><li>Otherwise, <strong>All Markdown files</strong> protects it.</li><li>Otherwise, <strong>Only matched paths</strong> requires an enabled <strong>Include</strong> match to protect it.</li></ol><p>New installations start in <strong>All Markdown files</strong> mode. Include rules are retained but inactive in that mode. In <strong>Only matched paths</strong>, an empty rule list protects nothing. Changing or deleting rules does not switch modes.</p><p>Each row has an <strong>Enabled</strong> checkbox, an <strong>Include</strong> or <strong>Exclude</strong> type, a <strong>Value</strong> field, and a Delete button. Disable a row to keep it for later without applying it. Disabled and unresolved rules do not participate in matching or active-rule counts.</p><p><img${ssrRenderAttr("src", _imports_0)} alt="Path rules showing vault paths, Obsidian URLs, and system paths"></p><h2 id="ordinary-vault-paths" tabindex="-1">Ordinary vault paths <a class="header-anchor" href="#ordinary-vault-paths" aria-label="Permalink to &quot;Ordinary vault paths&quot;">​</a></h2><p>By default, <strong>Use glob patterns</strong> is off and <strong>Case sensitive</strong> is on. Ordinary rules use literal path-prefix matching.</p><table tabindex="0"><thead><tr><th>Value</th><th>Use</th></tr></thead><tbody><tr><td><code>Reference/</code></td><td>Notes in Reference and all its subfolders</td></tr><tr><td><code>Archive/</code></td><td>Notes in Archive and all its subfolders</td></tr><tr><td><code>Notes/Summaries/</code></td><td>Notes under this nested folder</td></tr><tr><td><code>Reference/Handbook.md</code></td><td>A prefix beginning with this note path</td></tr></tbody></table><p>Keep the trailing <code>/</code> for folder rules. In prefix mode, a plain name without <code>.md</code> or wildcards is treated as a folder prefix after source resolution. When entering an existing vault path without an extension, the source resolver first checks for a matching Markdown note; an existing folder is normalized with a trailing slash. Use <code>/</code> explicitly if a note and folder share a name and you intend to select the folder.</p><p>A <code>.md</code> vault path remains a prefix rule, not an exact equality check. To target exactly one existing note regardless of prefix or glob mode, import its Obsidian URL or desktop system file path.</p><h3 id="editable-exceptions" tabindex="-1">Editable exceptions <a class="header-anchor" href="#editable-exceptions" aria-label="Permalink to &quot;Editable exceptions&quot;">​</a></h3><p>With <strong>Only matched paths</strong>, these rules protect reference notes but allow drafts:</p><table tabindex="0"><thead><tr><th>Type</th><th>Value</th></tr></thead><tbody><tr><td>Include</td><td><code>Reference/</code></td></tr><tr><td>Exclude</td><td><code>Reference/Drafts/</code></td></tr></tbody></table><p>With <strong>All Markdown files</strong>, an <strong>Exclude</strong> for <code>Inbox/</code> leaves that folder editable and protects other Markdown notes. An include rule cannot override an exclude.</p><h2 id="glob-patterns" tabindex="-1">Can I use wildcards in Obsidian read-only path rules? <a class="header-anchor" href="#glob-patterns" aria-label="Permalink to &quot;Can I use wildcards in Obsidian read-only path rules? {#glob-patterns}&quot;">​</a></h2><p>Yes. Turn on <strong>Advanced → Matching → Use glob patterns</strong> to enable the plugin&#39;s glob matcher for ordinary paths. Patterns match the entire vault-relative path.</p><table tabindex="0"><thead><tr><th>Pattern</th><th>Meaning</th></tr></thead><tbody><tr><td><code>**</code></td><td>All paths, including vault-root notes; protection still applies only to Markdown</td></tr><tr><td><code>Reference/*.md</code></td><td>Markdown notes directly inside Reference</td></tr><tr><td><code>Reference/**</code></td><td>Notes anywhere under Reference</td></tr><tr><td><code>Reference/**/Summary.md</code></td><td>Summary.md directly inside Reference or deeper below it</td></tr><tr><td><code>Notes/Summary?.md</code></td><td>One non-slash character after Summary, such as Summary1.md</td></tr></tbody></table><p><code>*</code> matches zero or more characters except <code>/</code>; <code>**</code> can cross folder separators; <code>?</code> matches one character except <code>/</code>. The <code>/**/</code> form can cover zero or more nested folders. A leading <code>**/README.md</code> requires a slash, so use <code>README.md</code> separately if you also want the vault-root note.</p><p>There is no regular-expression, brace-alternative, or character-class syntax. With glob matching off, <code>*</code> and <code>?</code> are literal characters. Switching matching modes affects ordinary saved path rules, so replace a prefix such as <code>Reference/</code> with <code>Reference/**</code> when enabling globs.</p><p>To protect the whole vault using <code>**</code>, follow <a href="./../guides/make-all-notes-read-only">Keep all Obsidian notes in Reading view</a>.</p><h2 id="case-sensitivity" tabindex="-1">Can I make note-path matching case-sensitive? <a class="header-anchor" href="#case-sensitivity" aria-label="Permalink to &quot;Can I make note-path matching case-sensitive? {#case-sensitivity}&quot;">​</a></h2><p>Yes. Open <strong>Settings → Read Only View → Advanced → Matching</strong> and turn <strong>Case sensitive</strong> on. It is enabled by default. Turn it off to ignore uppercase/lowercase differences when matching paths. This is a shared setting for include and exclude rules, not a per-rule option.</p><p>With <strong>Only matched paths</strong>, glob matching off, and an enabled Include <code>Reference/</code>:</p><table tabindex="0"><thead><tr><th>Note path</th><th>Case sensitive on</th><th>Case sensitive off</th></tr></thead><tbody><tr><td><code>Reference/Handbook.md</code></td><td>Read-only</td><td>Read-only</td></tr><tr><td><code>reference/Handbook.md</code></td><td>Editable</td><td>Read-only</td></tr></tbody></table><p>This example assumes no other rules match. The setting applies to prefix and glob matching, as well as comparisons against resolved exact-file rules. It does not rename files or make an invalid Obsidian URL or system path resolve: imported sources must still identify an existing item in the current vault. Verify the actual note path with <a href="./path-tester">Path tester</a>.</p><h2 id="temporarily-disable-rules" tabindex="-1">Can I temporarily disable a path rule without deleting it? <a class="header-anchor" href="#temporarily-disable-rules" aria-label="Permalink to &quot;Can I temporarily disable a path rule without deleting it? {#temporarily-disable-rules}&quot;">​</a></h2><p>Yes. Uncheck <strong>Enabled</strong> on the rule&#39;s row under <strong>Path rules</strong>. The plugin keeps its value and type, so you can re-enable it later without entering the path again.</p><p>To edit a note protected by its own include rule:</p><ol><li>In <strong>Only matched paths</strong>, find the note&#39;s <strong>Include</strong> row.</li><li>Uncheck that row&#39;s <strong>Enabled</strong> checkbox, leaving the global <strong>Enabled</strong> toggle on.</li><li>Wait for <strong>Saved.</strong> and test the note. If it is <strong>Editable</strong>, switch it into editing and make your changes.</li><li>Check the row&#39;s <strong>Enabled</strong> checkbox again and wait for <strong>Saved.</strong> to restore that rule.</li></ol><p>If another include, such as <code>Reference/</code> or <code>**</code>, also matches, disabling only the note rule will not make the note editable. Add an <strong>Exclude</strong> for the note instead. Keep that exclude row saved: enable it when editing and disable it when you want protection back. Disabling a folder include affects every note it was protecting.</p><p>In <strong>All Markdown files</strong>, include rows are already inactive. Use an exclude for a particular note, or use <strong>Disable read-only mode</strong> and <strong>Enable read-only mode</strong> to pause and restore protection globally. Neither approach needs you to delete saved paths, and protection does not resume on a timer.</p><h2 id="import-a-note-or-folder" tabindex="-1">Can I use an Obsidian URL, system path, or vault path in a rule? <a class="header-anchor" href="#import-a-note-or-folder" aria-label="Permalink to &quot;Can I use an Obsidian URL, system path, or vault path in a rule? {#import-a-note-or-folder}&quot;">​</a></h2><p>Yes. Add an enabled <strong>Include</strong> under <strong>Path rules</strong> in <strong>Only matched paths</strong>, then paste any of the following sources into <strong>Value</strong>. The field detects the source format. The same source formats also work for <strong>Exclude</strong> rules:</p><table tabindex="0"><thead><tr><th>Source</th><th>Example</th><th>Behavior</th></tr></thead><tbody><tr><td>Vault path</td><td><code>Reference/Handbook.md</code></td><td>Uses the configured prefix or glob matching</td></tr><tr><td>Obsidian URL</td><td><code>obsidian://open?vault=MyVault&amp;file=Reference%2FHandbook</code></td><td>Resolves one exact existing Markdown note in the current vault</td></tr><tr><td>Desktop system path</td><td><code>/Users/name/MyVault/Reference/Handbook.md</code></td><td>Imports one exact existing Markdown note inside the current vault</td></tr><tr><td>Desktop system folder</td><td><code>/Users/name/MyVault/Reference/</code></td><td>Imports a folder using ordinary folder matching</td></tr></tbody></table><p>Obsidian URLs may omit <code>.md</code>. Heading and block locators do not change which note is selected. A URL must name the current vault. Absolute system-path imports are available only on desktop, but the saved vault-relative path remains portable to mobile. The full system path is not persisted after successful import.</p><p>If a source cannot be resolved, its value remains visible with an explanation, but it does not match anything. Imported sources require existing targets and do not automatically follow later renames.</p><h2 id="verify-the-result" tabindex="-1">Verify the result <a class="header-anchor" href="#verify-the-result" aria-label="Permalink to &quot;Verify the result&quot;">​</a></h2><p>Wait for <strong>Saved.</strong>, then paste a specific Markdown note path into <a href="./path-tester">Path tester</a>. If the result is unexpected, check the selected mode, exclude matches, enabled rows, case sensitivity, and matching mode.</p><p>For very large configurations, settings show warnings and ignore rules beyond the limits: at most 200 includes, 300 excludes, and 400 total. Include rules are counted first; exclude rules at the end are trimmed if the total exceeds 400. Prefer a few folder rules where possible and review any limit warnings.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("docs/path-rules.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const pathRules = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  pathRules as default
};
